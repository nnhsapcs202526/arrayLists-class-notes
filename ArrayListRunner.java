import java.util.ArrayList;
/**
 * (write a descritpion of this class here)
 *
 * @author 
 * @version 
 */
public class ArrayListRunner
{
    public static void main( String[] args )
    {
        ArrayList<String> names = new ArrayList<String>();
        System.out.println( names );

        // a) Invoke add() to enter the following names in sequence: Alice, Bob, Connie, David, Edward, Fran, Gomez, Harry.
        //      Print the ArrayList to confirm the sequence.

        // b) Use get() to retrieve and print the first and last names in the list.

        // c) Print the size() of the ArrayList.

        // d) Use size() to help you print the last name in the list.
        
        // *** swap driver/navigator ***

        // e) Use set() to change Alice to Alice B. Toklas. Print the ArrayList to verify the change.

        // f) Use the alternate form of add() to insert Doug after David. Print the ArrayList again.

        // g) Use an enhanced for loop to print each name in the ArrayList.

        // h) Create a second ArrayList called names2 that is built by calling the ArrayList constructor that 
        //          accepts another ArrayList as an argument. Pass names to the constructor to build names2. 
        //          Then print the ArrayList to confirm it worked.

        // i) Call names.remove(0) to remove the first element. Print names and names2. 
        //          Verify that Alice B. Toklas was removed from names, but not from names2.

    }
}
